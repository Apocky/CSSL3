[package]
name         = "cssl-host-historical-reconstructor"
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
authors.workspace      = true
repository.workspace   = true
homepage.workspace     = true
description  = "Deterministic recompute of past-cohort events with TTL-tokenized 30-min tours (§ T11-W8-C4 · render-pipeline stub for cssl-render-v2/spectral/fractal-amp)"

[lints]
workspace = true

[dependencies]
blake3 = "1"
serde  = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
