# § T11-W13-W2 : cssl-host-weapons
# ══════════════════════════════════════════════════════════════════
# § I> POD-2 deliverable per Labyrinth/systems/weapons.csl  (W13-2)
# § I> 16 WeaponKind discriminants · hitscan + projectile + spline-bullet
# § I> COSMETIC-ONLY-AXIOM : DPS-parity-across-skins ; affixes = visual/audio
# § I> hitscan = single-frame raycast · multi-target pierce · damage-falloff
# § I> projectile = spline-trajectory (gravity+wind+drag) · ring-pool max-256-active
# § I> recoil-pattern emitted as event-stream (W13-5 ADS-recoil consumes ; we don't render)
# § I> accuracy-bloom : per-shot increase · time-decay recovery
# § I> pure-deterministic ; seeded-RNG ; replay-bit-equal ; #![forbid(unsafe_code)]
# § I> NO substrate-deps ; serde + serde_json only ; FFI-friendly Copy types
# § I> integration-points : combat-sim DamageRoll · gear-archetype RarityTier · audit-emit hook

[package]
name                   = "cssl-host-weapons"
description            = "CSSLv3 LoA-v13 weapons substrate : hitscan + projectile + spline-bullet · 16 WeaponKind · cosmetic-only-axiom DPS-parity · accuracy-bloom · damage-falloff · recoil-event-stream · projectile-ring-pool 256-cap · pure-deterministic seeded-RNG (W13-2)"
version                = "0.1.0"
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
authors.workspace      = true
repository.workspace   = true
homepage.workspace     = true

[lib]
name = "cssl_host_weapons"
path = "src/lib.rs"

[dependencies]
serde      = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true
